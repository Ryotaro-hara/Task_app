<footer>
    <h1>
        <% time = Time.now %>
        <% 
        year = time.year
        month = time.month
        day = time.day
        %>
        <%= "現在の日時 :" + year.to_s + "年" + month.to_s + "月" + day.to_s + "日" %>
    </h1>
</footer>